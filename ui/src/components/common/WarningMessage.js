import styled from 'styled-components';

const WarningMessage = styled.div`
  margin-top: 2rem;
  text-align: center;
  font-size: 22px;
  letter-spacing: 1px;
  font-weight: 600;
`;

export default WarningMessage;
